# deploy.yml — DISABLED (superseded by deploy-ec2.yml)
#
# This file previously used `docker compose up -d --build` which served
# the frontend on port 5173.  The production setup uses nginx (port 80)
# + PM2 backend, managed by deploy-ec2.sh via deploy-ec2.yml.
#
# Keeping this file here for reference only — the `on:` trigger has been
# removed so GitHub Actions will NOT pick it up.

name: "[DISABLED] Deploy via Docker Compose"

on:
  workflow_dispatch:   # manual only — not triggered on push
    inputs:
      confirm:
        description: "Type CONFIRM to run (this workflow is deprecated)"
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is disabled. Use deploy-ec2.yml instead."
